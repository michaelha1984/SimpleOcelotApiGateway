version: '3.4'

services:
  weatherforecast.api:
    image: ${DOCKER_REGISTRY-}weatherforecastapi
    build:
      context: .
      dockerfile: Services/WeatherForecast.API/Dockerfile
    ports:
      - "7001:443"